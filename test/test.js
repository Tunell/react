const chai = require('chai')
const expect = chai.expect

const

describe('Unit tests', () => {
  let usedMaterialAluminium,
    usedMaterialGolv,
    materials

  beforeEach(async () => {
    usedMaterialAluminium = require('./test-data/data').usedMaterialAluminium
    usedMaterialGolv = require('./test-data/data').usedMaterialGolv
    materials = require('./test-data/data').materials
  })

  it('should not do any unit conversion', () => {

  })
  it('should convert from m3 to kg', () => {

  })
  it('should convert from L to kg', () => {

  })
  it('should convert composite material materials to kg', () => {

  })
  it('should not do anything with composite material materials amounts', () => {

  })
  it('should compute correct result when calculating on a set of composite materials', () => {

  })
})
